easyblock = 'ConfigureMake'

name = 'fastp'
version = '1.0.1'

homepage = 'https://github.com/OpenGene/fastp'
description = """A tool designed to provide fast all-in-one preprocessing for FastQ files.
 This tool is developed in C++ with multithreading supported to afford high performance."""

toolchain = {'name': 'GCCcore', 'version': '12.3.0'}

source_urls = [GITHUB_SOURCE]
sources = ['v%(version)s.tar.gz']
checksums = ['80464cca840f7ecaeec63528cc5c4b138af83da909f91291115e1811e5f8cec6']

dependencies = [
    ('zlib', '1.2.11'),
    #('ISA-L', '2.30.0'),
    #('libdeflate', '1.7'),
]

buildopts = " CXX=${CXX}"

preinstallopts = "mkdir -p %(installdir)s/bin && "

installopts = 'PREFIX=%(installdir)s'

# https://github.com/OpenGene/fastp

github_account = 'OpenGene'

skipsteps = ['configure']

sanity_check_paths = {
    'files': ['bin/%(name)s'],
    'dirs': [],
}

sanity_check_commands = [('%(name)s', '--help')]

moduleclass = 'bio'
